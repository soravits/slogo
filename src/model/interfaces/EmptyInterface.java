package model.interfaces;

/**
 * This empty interface allows the program to hide the entirety of the model from general math and boolean
 * commands. An interface must be passed along with the input parameters to each command, so this interface
 * prevents general commands from accessing the Model
 * @author Brian
 *
 */
public interface EmptyInterface {

}
